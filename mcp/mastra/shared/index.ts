
import { shoppingListAgent } from '../verticals/shopping';
import { weatherAgent } from '../verticals/weather';

export const publicAgents = {
    weather: weatherAgent,
    shopping: shoppingListAgent
};