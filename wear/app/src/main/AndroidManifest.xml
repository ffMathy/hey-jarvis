<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- A watch app: the Play Store and the package manager keep it off phones. -->
  <uses-feature android:name="android.hardware.type.watch" />

  <application
    android:icon="@android:drawable/sym_def_app_icon"
    android:label="@string/app_name"
    android:theme="@android:style/Theme.DeviceDefault">

    <!-- Works without the phone app installed, which a prototype has to. -->
    <meta-data
      android:name="com.google.android.wearable.standalone"
      android:value="true" />

    <!--
      The whole of the assistant registration on a watch.

      There is no VoiceInteractionService here, unlike the phone app. Wear OS
      offers an app as the "Digital assistant app" when it has an activity that
      answers ASSIST — Home Assistant's watch app is registered exactly this way —
      and VOICE_ASSIST is included because that is the action some watches send
      for a long press rather than ASSIST. Which one arrived is shown on screen.
    -->
    <activity
      android:name=".AssistActivity"
      android:exported="true"
      android:launchMode="singleTask">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.ASSIST" />
        <action android:name="android.intent.action.VOICE_ASSIST" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </activity>

  </application>

</manifest>
