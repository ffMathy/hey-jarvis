{
  "name": "Hey Jarvis",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-20-bullseye",
  "features": {
    "ghcr.io/devcontainers-community/npm-features/typescript:1": {},
    "ghcr.io/devcontainers/features/python:1": {},
    "ghcr.io/devcontainers-extra/features/nx-npm:1": {},
    "ghcr.io/flexwie/devcontainer-features/op:1": {},
    "ghcr.io/jckimble/devcontainer-features/ngrok:3": {}
  },
  "containerEnv": {
    "ELEVENLABS_AGENT_ID": "${localEnv:ELEVENLABS_AGENT_ID}",
    "ELEVENLABS_API_KEY": "${localEnv:ELEVENLABS_API_KEY}",
    "GOOGLE_GENERATIVE_AI_API_KEY": "${localEnv:GOOGLE_GENERATIVE_AI_API_KEY}",
    "OPENWEATHERMAP_API_KEY": "${localEnv:OPENWEATHERMAP_API_KEY}",
    "VALDEMARSRO_API_KEY": "${localEnv:VALDEMARSRO_API_KEY}",
    "BILKA_EMAIL": "${localEnv:BILKA_EMAIL}",
    "BILKA_PASSWORD": "${localEnv:BILKA_PASSWORD}",
    "BILKA_API_KEY": "${localEnv:BILKA_API_KEY}",
    "ALGOLIA_API_KEY": "${localEnv:ALGOLIA_API_KEY}",
    "ALGOLIA_APPLICATION_ID": "${localEnv:ALGOLIA_APPLICATION_ID}",
    "BILKA_USER_TOKEN": "${localEnv:BILKA_USER_TOKEN}",
    "WIFI_SSID": "${localEnv:WIFI_SSID}",
    "WIFI_PASSWORD": "${localEnv:WIFI_PASSWORD}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack",
        "nrwl.angular-console",
        "1Password.op-vscode"
      ],
      "settings": {
        "typescript.preferences.preferTypeOnlyAutoImports": true,
        "typescript.suggest.autoImports": true
      }
    }
  },
  "containerUser": "root",
  "postCreateCommand": "npm install && nx run-many --target=init --all",
  "forwardPorts": [3000, 4200, 8080],
  "portsAttributes": {
    "3000": {
      "label": "E2E Application",
      "onAutoForward": "notify"
    },
    "4200": {
      "label": "Jarvis MCP Server",
      "onAutoForward": "notify"
    },
    "8080": {
      "label": "Voice Firmware Interface",
      "onAutoForward": "notify"
    }
  },
  "remoteUser": "node"
}