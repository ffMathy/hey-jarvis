# This file is generated by Turbo.
# Build the docker image with `bunx turbo build --filter=elevenlabs`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `bunx turbo run elevenlabs -p 3000:3000`.
#
# Pinned by digest so the base image is immutable; Dependabot keeps the tag and
# digest in sync. Keep the version aligned with `packageManager` in package.json.
FROM docker.io/oven/bun:1.3.9-alpine@sha256:9028ee7a60a04777190f0c3129ce49c73384d3fc918f3e5c75f5af188e431981

ENV HOST=0.0.0.0
ENV PORT=3000


WORKDIR /app

COPY . .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
#
# --frozen-lockfile installs exactly what bun.lock pins and fails if anything
# would have to be resolved fresh from the registry. Lifecycle scripts stay
# disabled through bunfig.toml.
RUN bun install --production --frozen-lockfile

CMD [ "bun", "run", "main.js" ]
