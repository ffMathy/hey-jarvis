# `android/` is generated by `expo prebuild`, like the phone app's.
/android/
/ios/
/.expo/

# Gradle output, for when a build has been run in place
/.gradle/
/.kotlin/

# Written by Android Studio with this machine's SDK path
/local.properties

# Signing material never belongs in the repository
*.jks
*.keystore
